# 主軸の成立率を計算する道具の設計

`pokemon-tcg-estimate-setup-rate` スキル([.claude/skills/pokemon-tcg-estimate-setup-rate/SKILL.md](../.claude/skills/pokemon-tcg-estimate-setup-rate/SKILL.md))と、計算の骨組み(`setup_rate/`)の設計の理由を記録する。手順そのものはスキルに書き、この文書には「なぜその作りにしたか」「何を扱わないと決めたか」「検証で何が分かったか」を書く。用語は [CONTEXT.md](../CONTEXT.md)「デッキ診断」の節に従う。

対応する Issue: [#22](https://github.com/mokosystem/pokemon-tcg-buddy/issues/22)。設計日: 2026-09-17。

2026-09-18 追記: この文書の「骨組みと規則ファイルの境界」で採った作り(デッキごとに規則ファイルを書く)は、利用者の多様なデッキに広げられないと分かり、[#27](https://github.com/mokosystem/pokemon-tcg-buddy/issues/27) で見直す。`setup_rate/` は置き換えまでの試作として残す。経緯は「今後の課題」の先頭にある。

## 目的

デッキ診断で「何番目の番までに主軸が立つか」「何番目の番までに主軸のワザを打てるか」を、根拠のある数字で示す。Issue 6 の試験診断では、この計算が先攻・後攻の差、枚数を変えたときの成立率の変化、失敗の要因の内訳を示し、カードを増減する判断の参考になった。一方で、その計算はそのデッキのために手順を書き下したもので、別のデッキに使うには手順を毎回書き直す必要があった([deck-diagnosis-design.md](deck-diagnosis-design.md)「今後の課題」)。

## 入力と出力

| 項目 | 内容 |
| --- | --- |
| 入力 | デッキコード(60 枚の内容)、狙い(何番目の番までに何ができていればよいか)、デッキごとの規則ファイル |
| 出力 | 狙いごとの成立率(先攻・後攻、締め切りごと)、失敗の要因の内訳、枚数を変えたときの成立率の比較、計算の前提 |

## 骨組みと規則ファイルの境界

### 汎用の道具にも、ひな型だけにもしなかった理由

Issue 22 の未確定の 1 つ目は「汎用の道具にするか、デッキごとに手順を書くひな型にとどめるか」だった。その中間を採った。

- どのデッキでも同じ部分は骨組みに固定する。山札を混ぜて配る、サイドを 6 枚置く、たねポケモンが無いときの引き直し、先攻・後攻で番を進める、基本ルールの制限(先攻の最初の番はサポートとワザを使えない、自分の最初の番は進化できない、出したばかり・進化したばかりのポケモンは進化できない、手札からのエネルギーとサポートとスタジアムは 1 番に 1 回、にげるは 1 番に 1 回)、乱数試行、締め切りごとの集計、枚数を変えたときの比較
- カードの効果を計算に落とした規則と、番ごとの優先順位は、デッキごとに規則ファイルに書く。カードテキストから機械的に作れないため、ここが汎用化の限界にあたる

全部を道具に閉じ込める案は、カードの効果を機械的に読む仕組み(カードデータベース)を自作することになり、README「情報源の方針」で決めた「公式サイトを一次情報とし、データベースを自作しない」に反する。ひな型だけの案は、基本ルールの制限を毎回書き直すことになり、規則の書き誤りが基本ルールの誤りと混ざる。

### 骨組みが提供するもの

| モジュール | 役割 |
| --- | --- |
| `setup_rate/cards.py` | カードの種別と、計算に使う属性(たね・進化、HP、タイプ、進化前、ルールを持つポケモンか、にげるエネルギー、エネルギーが供給するタイプ)。カードテキストは持たない |
| `setup_rate/state.py` | 自分側の対戦の状態と基本操作(引く、探す、ベンチに出す、エネルギーをつける、進化する、にげる、番を始める)。基本ルールに反する操作は `IllegalMove` を投げる |
| `setup_rate/engine.py` | 規則ファイルの行動の並びに従って 1 回の対戦を進める。行動は「実行できる最上位の 1 つを実行し、また最初から探す」を繰り返す |
| `setup_rate/simulate.py` | 乱数試行、締め切りごとの集計、失敗の要因の集計、枚数を変えたときの比較、表の整形 |
| `setup_rate/__main__.py` | コマンドラインの入口 |

行動を「優先順位の並び」にしたのは、Issue 6 の計算の前提「手札の使い方は固定の手順」をそのまま表せるため。手順を木の探索や最適化にすると、実際のプレイヤーの判断から離れた「計算上の最善」を成立率として示してしまう。優先順位の並びなら、履歴(`--trace`)を読んで「この番にこう動くのは実際の判断と違う」と検査できる。

### 規則ファイルが書くもの

カード表、60 枚の内容、対戦の準備、行動の並び、ワザの選び方、狙い、失敗の要因、枚数の変更案、前提。項目ごとの注意はスキルの手順 4 にある。

規則ファイルの判断には「山札とサイドの合計」(`count_unseen`)を使い、「山札だけ」(`count_in_deck`)を使わない決まりにした。プレイヤーは目当てのカードが山札にあるかサイドにあるかを知らないため、山札だけを見て判断すると、サイドにあるカードを探しに行く無駄をしない「計算上だけ強い」手順になる。山札を見てよいのは、検索の効果を実行するときと、失敗の要因の事後分析だけ。

## 計算の前提と限界

`pokemon-tcg-diagnose-deck` スキル「成立率を数字で示すとき」の 4 点をそのまま前提にする。

1. 相手の行動を含めない。相手の妨害、きぜつ、サイドを取ることは起きない。サイドが 6 枚のままなので、「サイドが 6 枚なら 8 枚引く」効果は常に 8 枚になる
2. 手札の使い方は規則ファイルの行動の並びに固定する。実際のプレイヤーの判断とは違うことがある
3. 締め切りは 2 番目の番と 3 番目の番の両方に置く
4. 計算の範囲(3 番目の番まで)の外で価値を持つカードには、数字が低くても減らしてよい根拠にならない

加えて、引き直しは自分の分だけ扱い、相手の引き直しで自分が追加で引ける 1 枚は扱わない。相手がいないため相手の引き直しの回数が決まらない。

## 公式サイトの内容を転載しない方針との整理

規則ファイルには、カードの効果を計算に落とした文(例: 「山札から HP 70 以下のたねを 2 枚までベンチに出す」)を書く。これは [pokemon-tcg/site-usage-notes.md](pokemon-tcg/site-usage-notes.md)「確認した事実を自分の言葉で書く」の範囲とし、カードテキストの引き写しはしない。カード表にはカード名、カード ID、種別と数値だけを持ち、効果の文章は持たない。ファイルの先頭に詳細ページの URL の形式と確認日を書く。

デッキの 60 枚の内容を規則ファイルに書くことは、環境スナップショットが「60 枚の内容は持たず、デッキコードから都度読む」としている方針と違う。検証に使った 6 デッキのうち、開発責任者のデッキ 5 つは本人の許可、ドラパルトex は公式の記事で公開された優勝デッキであるため、動作確認用の例としてリポジトリに置いた(Issue 22 決定事項)。他のユーザーのデッキの規則ファイルは、本人の許可が無い限りリポジトリに残さない。

## README「提供形態」との関係

README は「コーディングエージェント上で動くスキルとして提供する」を最初のゴールにしている。計算の骨組みはコードだが、提供形態はスキルのままで、コードはスキルが呼び出す道具と位置づける。Python 3.12 以上、依存パッケージ無し、環境管理は uv(`pyproject.toml`)。

2026-09-18 に、このリポジトリを Web(Next.js)、API(Bun と Elysia.js)、Terraform、AI エージェントのモノレポにする想定が README に書かれた。計算の骨組みはスキル、将来の API、模擬対戦(Issue 26)から使われる共有の部品にあたるため、Python のまま最上位に置く形は想定に合わない。Issue 27 で TypeScript の共有ライブラリとして `packages/` に作り直し、`setup_rate/` はそれまでの試作として残す。

## TypeScript の環境(Issue 27 の順 1)

2026-09-21 に整えた。以後の骨組みの移植(順 2)はこの環境の上で行う。

| 項目 | 内容 |
| --- | --- |
| 実行環境 | Bun 1.4.2。`mise.toml` の `[tools]` で固定し、`mise install` で入る |
| パッケージ管理 | Bun の workspaces(`packages/*`)。最上位の `package.json` は `private` で、`bun.lock` を 1 つだけ持つ |
| 計算の骨組みの置き場所 | `packages/solo-play-simulator`(パッケージ名 `@pokemon-tcg-buddy/solo-play-simulator`)。「一人回し」(相手なしでデッキを回すこと)を模擬する道具であることが名前から読めるようにした。当初の `setup-rate` は「成立率」の直訳で、準備の速さや割合と読めてしまい、デッキの計算だと分からないため改名した。この設計文書のファイル名と `pokemon-tcg-estimate-setup-rate` スキルの名前は、Issue 27 の順 9 の書き直しで合わせる |
| Linter と Formatter | Ultracite 7.12.0(Biome 2.5.12)。`biome.jsonc` は `ultracite/biome/core` だけを継承する |
| 静的解析 | fallow 3.27.0。`.fallowrc.json` は workspaces `packages/*` と、既定から変えた規則 3 つ(`unused-dev-dependencies`、`unused-optional-dependencies`、`private-type-leaks` を error)を書く。入口は各パッケージの `package.json` の `exports` から fallow が自動で検出する(順 2 で `src/index.*` の入口設定を外した。理由は下の「順 2 で決めたこと」) |
| 型検査 | TypeScript 7.0.2(devDependencies)。`bun test` は型を検査しないため、`tsc --noEmit -p tsconfig.json` を別に走らせる |
| 最上位の scripts | `bun run test`(`bun test`)、`bun run typecheck`(`tsc --noEmit`)、`bun run lint`(`ultracite check`)、`bun run lint:fix`(`ultracite fix`)、`bun run analyze`(`fallow`)。名前は何をするかで付け、道具が生成した `check` は使わない |
| 検査の自動実行 | `.github/workflows/ci.yml`。PR と main への push で、`test-packages`、`typecheck-packages`、`lint-packages`、`analyze-packages` の 4 つのジョブが並行して走る。各ジョブは jdx/mise-action が `mise.toml` から Bun を入れ、`bun install --frozen-lockfile` の後に `packages/` を対象に実行する。main への PR は、4 つのジョブの合格をルールセットで必須にする |

### 導入時に確かめたこと

- `npx ultracite init --quiet --linter biome --pm bun` の生成物は `biome.jsonc` と、`package.json` の devDependencies(ultracite、@biomejs/biome)と scripts(check、fix)だけだった。AGENTS.md、CLAUDE.md、`.vscode/` は書き換えられなかった。生成直後の `biome.jsonc` は Ultracite 自身の整形規則(配列を 1 行に畳む)に合わず `ultracite check` が落ちたため、`ultracite fix` で整えた
- fallow は `bunx fallow@latest --version` と `bunx fallow@latest recommend` の両方が Bun 1.4.2 で動いた(署名の検証も通った)。`recommend` が利用者に委ねる 2 つの選択(warn が既定の規則を CI で落とすか、同じファイルの非公開の型を公開した関数の型に使っている箇所を検査するか)は、順 1 では既定(落とさない、検査しない)のままにし、順 2 の 2 セッション目で決めた(下の「順 2 で決めたこと(2 セッション目)」)
- `tsconfig.json` は Bun の推奨設定(https://bun.com/docs/typescript 、確認日 2026-09-21)から、Web 向けの項目(`jsx`、`allowJs`)を除いたもの。`types` に `bun` を指定するため `@types/bun` を devDependencies に入れた
- Bun 1.4(2026-08-20 公開)の公開記事(https://bun.com/blog/bun-v1.4 、確認日 2026-09-21)には破壊的変更の節は無く、挙動が変わった点のうちこの使い方に関わるものも無かった。`trustedDependencies` が npm レジストリのパッケージにしか自動で効かなくなった点は、git や file 参照の依存を使わないため影響しない。`bun test --parallel` が `--isolate` を含むようになった点は、`--parallel` を使わないため影響しない。HTML ルートのソースマップ、HTTP/3、`--asset` はこのリポジトリの範囲外

### 順 2 で決めたこと(1 セッション目)

- **パッケージの公開の仕方は `package.json` の `exports` の下位パス(`./cards`、`./state`)にし、`src/index.ts` の再公開は置かない。** Ultracite の core プリセットは再公開だけのファイルを `noBarrelFile` で禁止している(束ねたファイルは、使わない部品まで読み込ませて起動を遅くするため)。プリセットの規則を上書きする案もあったが、この骨組みは Web(Next.js)と API(Elysia.js)から取り込まれる共有部品で、使う部品だけを読み込ませる利点がそのまま当てはまるため、規則に従った。使う側は `@pokemon-tcg-buddy/solo-play-simulator/state` のように部品ごとに取り込む。fallow は `exports` を入口として自動で検出し(`fallow list` で確認、2026-09-21)、入口のファイルから公開したクラスの部品は公開 API とみなして未使用の判定から外す
- **クラスの真偽値の欄は宣言で初期化せず constructor で代入する。** Biome は `hasUsedSupporter = false` を `false` 型と推論し、その欄を条件に使う箇所を `noUnnecessaryConditions` で「常に偽」と誤検出する(Biome 2.5.12、2026-09-21 に観測)。`hasUsedSupporter: boolean` と宣言して constructor で代入すれば、`noInferrableTypes` にも触れずに済む
- **型検査は CI に入れていない。** Bun のテスト実行は型を検査しないため、`tsconfig.json` の `strict` は CI では効いていない。順 2 の 1 セッション目では `bunx --bun tsc --noEmit -p tsconfig.json`(TypeScript 7.0.2)を手元で実行して通ることを確かめた。CI のジョブに足すかは順 2 の 2 セッション目で決める(足すなら main のルールセットの必須ジョブも更新する)。2 セッション目で足した(下の「順 2 で決めたこと(2 セッション目)」)

### 順 2 で決めたこと(2 セッション目)

2026-09-21。`engine.py` の準備部分と `simulate.py` を `engine.ts`・`simulate.ts` に移し、種を指定できる乱数の生成器を `random.ts` に足した。公開は `exports` の `./engine`、`./simulate`、`./random`。テストは `tests/test_state.py` の残り 4 件と `tests/test_deck_rules.py` の 6 件相当を含めて 54 件(Python は 25 件)。

- **行動を選ぶ部分は `PlayingPolicy`(対戦の準備の 2 つの選択、番の中の行動、ワザの選択、番の終わりの効果)として `runGame` に渡す。** Python の `DeckRules` は、カード表・60 枚の内容・行動の優先順位の並び・狙い・締め切り・変更案・前提を 1 つにまとめていたが、TypeScript では狙い(`Goal`)、締め切り(`Deadline`)、変更案(`DeckVariant`)、60 枚の内容(`Decklist`)を別の型にし、`DeckRules` と `run_actions`(優先順位の並びを上から試す繰り返し)は移さなかった。順 7 で探索が `PlayingPolicy` を実装する形に置き換えるため、優先順位の並びの仕組みを移しても捨てることになる。テストと答え合わせは、固定の手順を書いた小さな `PlayingPolicy` で回す
- **乱数は splitmix32(`createSeededRandom`)。** 出典は https://github.com/bryc/code/blob/master/jshash/PRNGs.md (確認日 2026-09-21)。同じ出典の mulberry32 は「32 ビットの値の 3 分の 1 を出さない」と記され、代わりに splitmix32 が勧められているため採らなかった。Python の `random.Random`(メルセンヌ・ツイスタ)の乱数列は再現しない(Issue 27 の決定事項「答え合わせの基準」)。Biome の `noBitwiseOperators` はこの関数の範囲だけ `biome-ignore-start`/`biome-ignore-end` で抑止した。`biome-ignore-all` はファイルの先頭にしか置けず、ファイルの説明コメントの後ろに置くと「先頭ではない」として無視される(Biome 2.5.12、2026-09-21 に観測)
- **枚数を変えたときの比較は、計算(`compareVariants`)と表の整形(`formatVariantComparison`)を分けた。** Python の `compare_variants` は計算しながら Markdown を組み立てていた。共有ライブラリでは、スキルは Markdown を、将来の API は数字をそのまま使うため、計算の結果を型(`VariantComparison`)で返し、整形を別の関数にした。`formatSummary` と `formatAssumptions` も同じ理由で、集計の型を受け取って文字列を返す純粋な関数にした
- **対戦の準備で使う 2 つの操作(`placeActiveFromHand`、`placePrizesFromDeck`)は `GameState` に足した。** Python の `setup_game` は `state.active` と `state.hand` を直接書き換えていた。山札と手札の書き換えを `GameState` の外に置くと、基本ルールの検査(たねポケモンだけをバトル場に出せる)がすり抜けるため
- **テスト用のカード表は `sample-cards.ts` に 1 つ置き、3 つのテストが共有する。** テストごとにカード表を書くと、fallow の重複検出(既定の mild、50 トークン以上が 2 回)に掛かる
- **型検査を CI に足した。** ジョブは `typecheck-packages`、script は `typecheck`、`typescript` 7.0.2 を devDependencies に入れた。`bun test` は型を検査しないため、`strict` と `noUncheckedIndexedAccess` の違反が手元の `tsc` でしか見つからず、CI を通った変更が使う側(Web、API)のビルドで初めて落ちる。main のルールセット(id 23753240)の必須ジョブにも足した
- **fallow の保留 2 点を決めた。** (1) 既定で warn の規則のうち、このリポジトリに関係する `unused-dev-dependencies` と `unused-optional-dependencies` を error にした。UI フレームワークや CSS が無いため、部品(component)や CSS の規則は対象にならない。使われない開発用の依存は `bun install` の時間と取り込む供給網の面積を増やすだけで、残す理由が無い。(2) `private-type-leaks` を error で有効にした。共有ライブラリで、公開した関数の型に同じファイルの非公開の型が漏れると、使う側がその型を書けない。fallow の提案は warn だが、warn は CI を止めないので気づく機会が無い。有効にして指摘は 0 件だった。規則名は fallow の設定スキーマ(`node_modules/fallow/schema.json` の `rules.default`、fallow 3.27.0)と公式文書(https://docs.fallow.tools/configuration/overview 、確認日 2026-09-21)で確認した
- **ライブラリは実行環境に依存しない。** `src/` のテスト以外のファイルの import は同じディレクトリのモジュールだけで、`node:`、`bun:`、`process`、`Bun`、ファイルの読み書きを使っていない(`grep` で確認、2026-09-21)。`bun:test` はテストだけが使う

### 採らなかった案

- **oven-sh/setup-bun で Bun を入れる案。** `bun-version-file` が読めるのは `package.json`、`.bun-version`、`.tool-versions` で、`mise.toml` は読めない(https://github.com/oven-sh/setup-bun 、確認日 2026-09-21)。Bun の版を `mise.toml` と別の場所にも書くと二重管理になるため、`mise.toml` を読む jdx/mise-action を使う
- **テスト、Ultracite、fallow を 1 つのジョブ `check` にまとめる案。** 依存パッケージの取得が 1 回で済むが、`check` は何をするか、リポジトリ全体か `packages/` 配下かが名前から読めない(モノレポでは範囲の区別が要る)。ジョブを「何をするか-どの範囲か」(`test-packages` など)の 3 つに分け、名前だけで内容と範囲が分かる形にした。取得は Bun のキャッシュで数秒なので、3 回になっても支障は無い

## 検証の記録

Issue 22 の完了の定義に対応する記録。いずれも 2026-09-17 に、乱数の種 20260917、試行 20000 回で実行した。枚数を変えたときの比較は同じ種で回すので、差は試行の揺れではなく変更の効果として読める。20000 回のときの割合の揺れはおよそ ±0.7 ポイント(95 % の範囲)。

全カードの効果は、この日に公式のカード詳細ページで確認した(デッキごとの規則ファイルの先頭にカード ID と確認日がある)。

### 手順 2: Issue 6 の計算の再現(メガサーナイトex)

題材: 開発責任者のメガサーナイトex デッキ。Issue 6 で計算した当時のデッキコード xG8Kax-DHob4e-84xcca(規則ファイル `mega_gardevoir_ex.py`)と、現在のデッキコード vFkFVV-JqbSlh-w5kbbd(`mega_gardevoir_ex_current.py`。違いはリーリエのピッピex 2→1、ミュウex 1→2)。

| 狙い(3 番目の番まで) | Issue 6 の記録 | 今回(当時の構築) | 今回(現在の構築) |
| --- | --- | --- | --- |
| メガシンフォニアを打てる(先攻 / 後攻) | 82 % / 84 % | 79.8 % / 84.7 % | 80.4 % / 84.9 % |
| 300 以上で打てる(先攻 / 後攻) | 54 % / 61 % | 47.8 % / 64.2 % | 48.0 % / 64.2 % |
| キルリア 3→2 の変化 | 4 ポイント低下 | 3〜4 ポイント低下 | - |

Issue 6 の記録とおおむね一致したが、先攻の「300 以上」が 6 ポイント低い。Issue 6 の計算は作業用の一時領域に置いたもので手順が残っておらず、今回の規則ファイルの優先順位が当時と同じではないことによる差と考えられる。どちらが実際のプレイに近いかは、開発責任者の判断を仰ぐ。

2 番目の番までにメガサーナイトex が立つ割合は先攻 59.0 % / 後攻 72.3 %。失敗の要因の上位は「偉大な大樹が無く、ふしぎなアメとメガサーナイトex が揃わない」「偉大な大樹を手札に引けていない」「偉大な大樹がサイドにある」で、主軸への道が偉大な大樹に集中していることが数字に出る。

### 手順 3: 狙いの形が違う 3 デッキ

| デッキ | 狙い | 先攻 | 後攻 |
| --- | --- | --- | --- |
| メガリザードンYex & Xex(`mega_charizard_ex.py`) | 1 番目の番にファイアローex でかぎづめハント | (先攻はワザを使えない) | 17.6 % |
| 同上 | 2 番目の番までにかぎづめハント | 22.1 % | 31.1 % |
| 同上 | 3 番目の番までにメガリザードンのワザを打てる | 29.3 % | 42.4 % |
| ばけがくれ(`bakegakure.py`) | 3 番目の番までにトラッシュに「ばけがくれ」4 枚 | 61.4 % | 71.2 % |
| 同上 | 3 番目の番までにむねんのイカリを 170 で打てる | 52.2 % | 61.0 % |
| ドラパルトex(`dragapult_ex.py`) | 2 番目の番までにドラパルトex が場にいる | 20.8 % | 28.2 % |
| 同上 | 3 番目の番までにファントムダイブを打てる | 52.8 % | 62.7 % |

3 デッキとも規則ファイルを書き足すだけで動いたが、骨組みの基本操作を 6 つ足した(次節)。優先順位の妥当性(実際のプレイヤーの判断と合っているか)は、開発責任者の確認を経ていない。数字は「この規則ファイルの手順で回したときの割合」として読む。

### 枚数を変えたときの比較で分かったこと

各規則ファイルの変更案を `--compare` で回した結果の要点。数字は「3 番目の番まで」の狙いに対する変化(先攻 / 後攻、ポイント)。抜いた枚数は基本エネルギー 1 枚で埋めた。

| デッキ | 変更 | 変化 | 読み方 |
| --- | --- | --- | --- |
| メガサーナイトex | キルリア 3→2 | メガシンフォニアを打てる -2.9 / -2.3、場にいる -3.4 / -3.0 | Issue 6 で開発責任者が退けた提案と同じ向き。2 番目の番の成立率も -3.1 / -3.3 |
| メガサーナイトex | ふしぎなアメ 2→1、アクロマの執念 4→3 | 2 番目の番に場にいる -4 前後 | 主軸への道の必要カードは、1 枚減らすと 2 番目の番の数字に先に出る |
| メガサーナイトex | ミュウex 1→0 | ±0.4 以内 | ミュウex の価値(ベンチのメガサーナイトex のワザを使う)は、規則の手順ではほとんど使われなかった。計算の範囲の外(相手にバトル場のポケモンを倒された後)で価値を持つカードの例 |
| メガリザードン | イグニッションエネルギー 3→4(基本炎 -1) | かぎづめハント 1 番目 +3.0(後攻) | 後攻 1 番目の番の狙いには効く。メガリザードンのワザには影響しない |
| メガリザードン | ふしぎなアメ 2→3 | メガリザードンが 2 番目に場にいる +5.1 / +6.3 | 3 番目の番のワザは +1.6 / +2.3 にとどまる |
| メガリザードン | ヒカリ 2→1 | メガリザードンが 3 番目に場にいる -3.2 / -5.5 | ヒカリはこの規則で主軸の線を揃える主な手段 |
| ばけがくれ | ジュペッタ 3→2 | 170 で打てる -6.1 / -4.3 | ジュペッタは「ばけがくれ」であり、かつ進化ポケモンなのでポフィンやテレパスで場に出ず、手札に留まってトラッシュに送りやすい |
| ばけがくれ | ノココッチ 2→1 | 170 で打てる -5.7 / -5.6 | 引く枚数がトラッシュの枚数を支えている |
| ばけがくれ | プリズムタワー 3→2、ハイパーボール 4→3 | トラッシュ 4 枚(3 番目)-5.3 / -4.4、-5.0 / -3.3 | 捨てる手段が直接効く |
| ドラパルトex | アカマツ 2→1 / 2→3 | ファントムダイブ -4.6 / -3.8、+4.7 / +5.8 | 山札からエネルギーをつけるサポートが 3 番目の番のワザを左右する |
| ドラパルトex | ふしぎなアメ 2→1 | ドラパルトex が 2 番目に場にいる -9.3 / -12.4、しかしファントムダイブ(3 番目)は +5.0 / +6.1 | **規則の手順の癖が出た例。** アメとドラパルトex を手札に持つとリーリエの決心で手札を流さない決まりにしているため、アメが減るとリーリエを使う対戦が増え、エネルギーが揃いやすくなった。アメを減らす根拠にはならず、規則の「リーリエを使わない条件」を見直す材料として扱う |

最後の行のように、枚数を変えたときの比較は規則ファイルの手順の癖をそのまま映す。変化が直感と逆のときは、まず失敗の要因の内訳と履歴を読み、手順の癖なのかデッキの性質なのかを見分けてから提示する。

### 骨組みに手を入れた記録

完了の定義「骨組みに手を入れず規則を書き足すだけで動く。動かなければ骨組みを直し、理由を設計文書に残す」に対応する。いずれも「基本操作」の追加で、番の進行や集計は変えていない。

| 追加した操作 | 必要になったカード | 理由 |
| --- | --- | --- |
| 番の終わりの処理(`DeckRules.end_turn`) | イグニッションエネルギー(番の終わりにトラッシュ) | 骨組みの番の進行に「番の終わり」の呼び出し口が無かった |
| ワザのエネルギーを払えるかの判定(`can_pay_cost`) | メガリザードン(炎 2 個)、ファイアローex(無色 2 個をイグニッション 1 枚で払う) | メガサーナイトex は超 1 個で足りたため、タイプ指定と無色の組み合わせを判定する仕組みが無かった |
| 効果で進化ポケモンを直接ベンチに出す(`place_on_bench(by_effect=True)`) | ファイアローex(特性でベンチに出す) | ベンチに出せるのはたねポケモンだけ、という検査が効果を妨げた |
| 山札の上から見て 1 枚を手札に加え、残りを戻して切る(`reveal_top_and_take`) | ポケギア3.0 | 山札を検索する操作しか無かった |
| 場のポケモンをついているカードごと山札に戻す(`return_pokemon_to_deck`) | ノココッチ(特性で山札に戻る) | 場からカードを取り除く操作が無かった |
| 山札の上から見て 1 枚を手札に、残りを山札の下に戻す(`look_top_take_one`) | ドロンチ(特性「ていさつしれい」) | 残りを「切る」のではなく「下に戻す」ため、上の操作と区別した |

### 規則を書くときに見つけた書き誤り

規則ファイルは、書いた直後は必ずどこかが実際の判断と違う。履歴(`--trace`)と、失敗の要因ごとに対戦の履歴を抽出して読むことで見つけた書き誤りを、スキルの手順 5 の検査項目にした。

- 先攻の最初の番に、手札から出したときの特性を持つポケモン(ニャースex)を特性を使わずに出していた。サポートを使えない番は手札に残すべきだった
- エネルギーを、偉大な大樹で進化させる前の別のラルトスにつけていた。サポート(アクロマの執念)で偉大な大樹を取りに行く可能性を先に試してから、進化した主軸につけるべきだった。ただしリーリエの決心で手札を流す番は、流す前につける
- 手札のメガサーナイトex を、エネルギーがついたキルリアに乗せる前に、偉大な大樹で別のラルトスの線を作っていた
- ハイパーボールで探す相手の一覧に、サポートを探す線(ニャースex)が無かった
- 番の終わりにトラッシュする特殊エネルギー(イグニッション)を、その番にワザを打てない(バトル場に出せない)のにつけていた
- ポケモンいれかえが無いデッキ(ドラパルトex)で、バトル場の主軸でないポケモンに要らないエネルギー(基本悪エネルギー)をつけてにげる線が無く、「バトル場に出せない」が 2 割を超えていた
- バトル場のノココッチが特性で山札に戻ると場が空になる対戦で、骨組みが止まった。ベンチが空のときは特性を使わない条件を規則に足した

## 扱わないと決めたこと

- **相手の行動を含めた計算。** 相手の妨害、きぜつ、サイドの進みは前提が多く、根拠を示せない。相手を含めた計算は模擬対戦の調査(Issue 26)の範囲にあたる
- **手順の最適化や探索。** 「計算上の最善」ではなく「固定した手順」で回す。理由は「骨組みが提供するもの」に書いた。2026-09-18 に Issue 27 でこの決定を覆した。固定した手順をデッキごとに人が書く量が、利用者の多様なデッキに対して現実的でないため
- **4 番目の番より後。** 終盤の動きは山札の残りと相手の行動に強く依存し、相手を含めない計算では意味のある数字にならない
- **カードの効果を機械的に読む仕組み。** README「情報源の方針」の「データベースを自作しない」に従う。カードテキストを機械的に読む仕組みは Issue 27 でも作らない。Issue 27 で持つのは、公式の内容を読んで計算用に翻訳した結果(効果の記法で書いた効果の翻訳)と、公式 Q&A への参照であり、公式の内容の転載ではない

## 今後の課題

- **規則ファイルの形の限界(Issue 27 へ引き継ぐ)。** 4 デッキの規則ファイルは 1 デッキあたり 500〜700 行になり、その大半はカードの効果ではなくプレイングの判断基準(何を捨て、何を探し、誰をバトル場に出すか)だった。効果そのものは「引く、山札から条件で探す、手札から捨てる、山札の上から見て取る、山札に戻す、エネルギーをつける、進化させる」と条件の組み合わせで書けている。利用者のデッキは多種多様で、デッキの種類ごとにこの量を書く運用は成り立たない。Issue 27 では、効果の翻訳を効果の記法で書くデータにし、解釈と裁定をカード ID ごとのデータにし、プレイングの判断基準を「宣言した狙いの成立確率を最大にする手を探索で選ぶ」に置き換える。デッキごとの入力は宣言(主軸への道、狙いの 3 段)だけにする
- **TypeScript への移行(Issue 27 へ引き継ぐ)。** 上の「README「提供形態」との関係」の通り、共有の部品は TypeScript で `packages/` に置く。移すのは骨組み(状態と番の進行、乱数試行、集計)で、規則ファイルは移さない。4 デッキの規則ファイルと「検証の記録」の数字は、同じ乱数の種と枚数の変更案で出し直して答え合わせに使う
- **優先順位の妥当性の確認。** 4 デッキの規則ファイルの行動の並びは、開発責任者の実際の判断と照らし合わせていない。Issue 27 では、探索が選んだ手と人の判断がずれた場面を集め、評価の抜けか宣言の不足かを切り分ける材料にする
- **追試(シロナのガブリアスex、メガレックウザex)。** Issue 22 の完了の定義には含めず、スキルの手順だけで規則を書けるかの追試に使う
- **デッキの回し方の説明(Issue 25)との関係。** 規則ファイルの行動の並びは「番ごとに何を優先するか」そのもので、回し方の説明の材料になる
- **ドラパルトex の規則の「リーリエの決心を使わない条件」。** ふしぎなアメを減らすとファントムダイブの成立率が上がる結果は、手順の癖による。アメとドラパルトex を持っていてもエネルギーが無ければリーリエの決心を使う、などの条件を検討する
- **数字の提示の仕方。** 失敗の要因の内訳は規則ファイルの書き方で粒度が変わる。診断の会話でどこまで見せるかは、実際の相談で確かめる
